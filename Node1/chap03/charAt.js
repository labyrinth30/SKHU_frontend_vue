let s = "hello world";
for (let i = 0; i < s.length; ++i)
console.log("%s %s %d", s[i], s.charAt(i), s.charCodeAt(i));


// 문자열[위치]
// 문자열에서 위치(index)의 문자를 리턴한다.
// 문자열.charAt(위치)
// 문자열에서 위치(index)의 문자를 리턴한다.
// 문자열.charCodeAt(위치)
// 문자열에서 위치(index)의 문자의 코드 값을 리턴한다.