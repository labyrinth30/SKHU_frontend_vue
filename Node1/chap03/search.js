let s = "hello World";
console.log(s.search(/[A-Z]/));

// 정규식(regular expression)
// 정규식은, 패턴으로 문자열을 찾기 위한 문자열 패턴 표현식이다.
// [A-Z] 정규식은 A부터 Z까지 문자 중 아무 한 문자를 의미한다.
// 즉 대문자 한 개와 일치하는 정규식이다.
// 정규식에 대한 설명은 부록에서... (부록은 학습 목표에 포함 안됨)